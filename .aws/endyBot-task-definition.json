{
  "family": "endyBot",
  "containerDefinitions": [
    {
      "name": "endyBot",
      "image": "",       // how does interjecting our image work?
      "cpu": 1024,
      "entryPoint":["node ./endyBot"],
      "command":[""],     // would we be okay with just a command
      "portMappings": [
        {
          "protocol": "tcp",    
          "containerPort": 80
        }
      ],
      "memory": 1024,
      "essential": true
    }
  ],
  "networkMode": "awsvpc",       // look into network settings (NAT possibly)
  "memory": "1024",
  "cpu": "1024"
}